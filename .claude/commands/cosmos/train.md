---
description: "COSMOS /cosmos:train — run KB ingestion pipeline (embeddings, graph nodes, eval score)"
allowed-tools: all
---
Read CLAUDE.md to load COSMOS context.
Read .claude/commands/cosmos.md for this command's exact process specification.
Execute: /cosmos:train $ARGUMENTS — follow the exact process defined, no shortcuts.
